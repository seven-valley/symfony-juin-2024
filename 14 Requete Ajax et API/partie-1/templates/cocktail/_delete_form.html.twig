<form method="post" action="{{ path('app_cocktail_delete', {'id': cocktail.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cocktail.id) }}">
    <button class="btn">Delete</button>
</form>
