{% extends 'base.html.twig' %}

{% block title %}Edit Cocktail{% endblock %}

{% block body %}
<div class="container">
<div class="col-6">
    <h1>Edit Cocktail</h1>
   {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}


    <a href="{{ path('app_cocktail_index') }}">back to list</a>

    {{ include('cocktail/_delete_form.html.twig') }}
    <div>
    </div>
{% endblock %}
