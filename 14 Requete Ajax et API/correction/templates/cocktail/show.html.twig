{% extends 'base.html.twig' %}

{% block title %}Cocktail{% endblock %}

{% block body %}
    <h1>Cocktail</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cocktail.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ cocktail.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cocktail_index') }}">back to list</a>

    <a href="{{ path('app_cocktail_edit', {'id': cocktail.id}) }}">edit</a>

    {{ include('cocktail/_delete_form.html.twig') }}
{% endblock %}
